# System aliases
alias upgrade="sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y"
alias ..="cd .."

# Git aliases
alias gs='git status'
alias gr='git restore'
alias gl='git l'
alias gg='git pull'
